# Copy to .env and fill in. .env is gitignored; .env.example is committed.

# Miners: HuggingFace write token, used by `glyph-miner publish` to create/push your codec
# repo (write scope). Validators: optional but recommended -- avoids intermittent
# 403/AccessDenied from HF's Xet-backed CDN throttling fully anonymous traffic while
# streaming the round corpus (issue #108); a free-tier read token is enough, no special
# dataset permissions needed. Create one at https://huggingface.co/settings/tokens.
# HF_TOKEN=hf_...

# Optional: only needed if running the validator with --runner chutes (the default eval path
# is local Docker on an RTX 4090 -- see README). Chutes (SN64) cpk_ invocation key used by the
# validator's ChutesRunner. Create one with: chutes keys create --name glyph-validator --admin
# CHUTES_API_KEY=cpk_...

# Optional: the Chutes account that deployed the eval chutes. Used to build the default
# compress/decompress chute URLs and as the build/deploy username. Defaults to "glyph"; set
# if you deployed under a different account.
# GLYPH_CHUTE_USERNAME=glyph

# Optional: override the deployed chute base URLs outright (take precedence over
# GLYPH_CHUTE_USERNAME). Compress and decompress are SEPARATE chutes; point every validator at
# the same two deployed chutes.
# GLYPH_COMPRESS_CHUTE_URL=https://<account>-glyph-compressor.chutes.ai
# GLYPH_DECOMPRESS_CHUTE_URL=https://<account>-glyph-decompressor.chutes.ai

# Optional: blockmachine.io archive API key (paid plan), asked for during install. Makes
# blockmachine the preferred archive source for the conviction-ledger backfill (issue #151):
# ~1-3s per historical metagraph query vs ~20-30s (and frequent overloads) on the public
# archive node -- a fresh backfill drops from ~40+ minutes to ~3. Purely a local speed
# preference (historical chain state is identical from any honest archive); leave unset to
# use the public archive node.
# BLOCKMACHINE_API_KEY=...
